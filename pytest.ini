# Lets `pytest` run from the repo root (server/pytest.ini covers runs from server/).
# Without this, no config is found from the root and async tests all fail because
# asyncio_mode defaults to strict.
[pytest]
asyncio_mode = auto
testpaths = server/tests
